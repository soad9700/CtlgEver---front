import React, { Component } from 'react';
import './App-Bar.scss';

class AppBar extends Component {

    render() {
        return (
            <div id="AppBar">app bar here!</div>
        );
    }
}

export default AppBar;